import React from 'react'
import ReactDOM from 'react-dom/client'
import DesignInterface from './DesignInterface.tsx'
import '../styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DesignInterface />
  </React.StrictMode>,
)
